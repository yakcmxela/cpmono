{
  "routes": [
    {
      "method": "GET",
      "path": "/venues",
      "handler": "venue.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venues/count",
      "handler": "venue.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/venues/:id",
      "handler": "venue.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/venues",
      "handler": "venue.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/venues/:id",
      "handler": "venue.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/venues/:id",
      "handler": "venue.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
