import { AccountLayout } from "layouts/AccountLayout";

const AccountEventsList = ({ route }) => {
  return (
    <AccountLayout route={route}>
      <main>...</main>
    </AccountLayout>
  );
};

export default AccountEventsList;
