import { AccountLayout } from "layouts/AccountLayout";

const AccountEventEdit = ({ route }) => {
  return (
    <AccountLayout route={route}>
      <main>...</main>
    </AccountLayout>
  );
};

export default AccountEventEdit